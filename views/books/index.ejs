<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body style="background-image: linear-gradient(to right top, #c65d2a, #d27421, #db8c15, #dfa505, #debf00, #ddc004, #dcc208, #dbc30c, #daac14, #d5971e, #cd8226, #c16f2d); background-attachment: fixed;" class="container" style="margin-top: 50px;">

    <div class="container p-5">
        <h1 class="text-center mb-5 text-uppercase text-muted">MANGA WORLD</h1>

    <% if (messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>
    
    <% if (messages.success) { %>
        <div class="alert alert-success" role="alert"><%= messages.success %></div>
    <% } %>

    <div class="card">
        <div class="card-header">
            <ul class="nav nav-pills w-70">
                <h1 class="nav-link">🐣Manga漫画🐥</h1>
                <h6 class="nav-link">🐻(子どもKodomo)</h6>
                <h6 class="nav-link">🐼(少年Shounen)</h6>
                <h6 class="nav-link">🐻‍❄️(成年Seinen)</h6>
                <li class="nav-link ml-auto">
                    <a href="/books/add" class="nav-link active">Add Manga📖 </a>
                </li>
            </ul>
        </div>
        <div class="card-body">
            <% if(data.length) { %>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Name📚</th>
                            <th scope="col">Author👤</th>
                            <th width="200px">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(let i = 0; i < data.length; i++) { %>
                            <tr>
                                <th scope="row"><%= data[i].id %></th>
                                <td><%= data[i].name %></td>
                                <td><%= data[i].author %></td>
                                <td>
                                    <a href="../books/edit/<%= data[i].id %>" class="btn btn-success edit">Edit📝</a>
                                    <a href="../books/delete/<%= data[i].id %>" onclick="return alert('Are you sure?')" class="btn btn-danger delete">Delete🗑️</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            <% } %>

            <!-- if result is empty -->
            <% if(!data.length) { %>
                <p class="text-center">เเนะนำ Manga หน่อยสิ!!!</p>
            <% } %>
        </div>
    </div>

</body>
</html>